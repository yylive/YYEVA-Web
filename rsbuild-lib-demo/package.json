{
  "name": "rsbuild-lib-demo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "rsbuild build",
    "dev": "rsbuild dev --open",
    "format": "prettier --write .",
    "preview": "rsbuild preview"
  },
  "devDependencies": {
    "@rsbuild/core": "^0.7.7",
    "prettier": "^3.1.1",
    "typescript": "^5.4.2"
  },
  "dependencies": {
    "yyeva": "workspace:^"
  }
}
