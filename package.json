{
  "name": "yy-eva-workspace",
  "scripts": {
    "dev": "pnpm --filter={yyeva,offical} dev",
    "build": "pnpm --filter={yyeva,offical} build",
		"start": "pnpm --filter=offical start"
  },
  "devDependencies": {
    "@efox/eslint-config-react": "^2.3.0"
  }
}
