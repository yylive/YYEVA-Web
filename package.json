{
  "name": "yy-eva-workspace",
  "scripts": {
    "dev": "pnpm --filter=yyeva --filter=offical dev",
    "build": "pnpm --filter=yyeva --filter=offical build",
    "start": "pnpm --filter=offical start"
  },
  "devDependencies": {
    "@empjs/eslint-config-react": "^1.0.0"
  }
}
